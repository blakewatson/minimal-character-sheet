---
phase: quick
plan: 260321-pi3
type: execute
wave: 1
depends_on: []
files_modified:
  - css/vendor/quill.bubble.css
  - css/vendor/baguetteBox.min.css
  - css/vendor/gutenberg.min.css
  - css/print.css
  - css/spacing.css
  - templates/header.html
  - templates/print.html
  - vite.config.js
  - public/quill.bubble.css
  - public/baguetteBox.min.css
  - public/gutenberg.min.css
  - public/print.css
  - public/spacing.css
  - public/flex.css
  - public/reset.min.css
  - dist/flex.css
  - dist/reset.min.css
autonomous: true
must_haves:
  truths:
    - "Vendor CSS (quill, baguetteBox, gutenberg) is served from css/vendor/ and loads correctly in the browser"
    - "print.css and spacing.css source files live in css/ and are output to dist/ by Vite build"
    - "Unreferenced flex.css and reset.min.css are deleted from both public/ and dist/"
    - "All pages render with correct styles — no broken CSS references"
  artifacts:
    - path: "css/vendor/quill.bubble.css"
      provides: "Quill editor bubble theme styles"
    - path: "css/vendor/baguetteBox.min.css"
      provides: "Lightbox gallery styles"
    - path: "css/vendor/gutenberg.min.css"
      provides: "Print typography styles"
    - path: "css/print.css"
      provides: "Custom print stylesheet source"
    - path: "css/spacing.css"
      provides: "Custom spacing utilities source"
  key_links:
    - from: "templates/header.html"
      to: "css/vendor/quill.bubble.css"
      via: "link href"
      pattern: "css/vendor/quill\\.bubble\\.css"
    - from: "templates/header.html"
      to: "css/vendor/baguetteBox.min.css"
      via: "link href"
      pattern: "css/vendor/baguetteBox\\.min\\.css"
    - from: "templates/print.html"
      to: "css/vendor/gutenberg.min.css"
      via: "link href"
      pattern: "css/vendor/gutenberg\\.min\\.css"
    - from: "vite.config.js"
      to: "css/print.css"
      via: "rollup input"
      pattern: "print.*css/print\\.css"
---

<objective>
Move non-generated CSS files out of dist/ and public/ into proper source locations. Vendor CSS goes to css/vendor/ (served directly by PHP). Hand-written CSS (print.css, spacing.css) goes to css/ as source files and gets output to dist/ via Vite rollup inputs. Unreferenced CSS files are deleted.

Purpose: Clean up the build output directory so dist/ only contains Vite-generated artifacts and public/ copies that are intentional.
Output: Properly organized CSS source files, updated template references, updated Vite config.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@css/app.css
@templates/header.html
@templates/print.html
@vite.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move CSS files to proper source locations</name>
  <files>css/vendor/quill.bubble.css, css/vendor/baguetteBox.min.css, css/vendor/gutenberg.min.css, css/print.css, css/spacing.css</files>
  <action>
1. Create css/vendor/ directory.

2. Move vendor CSS from public/ to css/vendor/:
   - cp public/quill.bubble.css css/vendor/quill.bubble.css
   - cp public/baguetteBox.min.css css/vendor/baguetteBox.min.css
   - cp public/gutenberg.min.css css/vendor/gutenberg.min.css

3. Move hand-written CSS from public/ to css/:
   - cp public/print.css css/print.css
   - cp public/spacing.css css/spacing.css

4. Delete ALL CSS files from public/ (vendor, hand-written, AND unreferenced):
   - rm public/quill.bubble.css
   - rm public/baguetteBox.min.css
   - rm public/gutenberg.min.css
   - rm public/print.css
   - rm public/spacing.css
   - rm public/flex.css
   - rm public/reset.min.css

5. Delete unreferenced CSS from dist/ (build artifacts will be regenerated):
   - rm dist/flex.css
   - rm dist/reset.min.css

Note: Do NOT delete other dist/*.css files yet -- they will be regenerated by the next Vite build.
  </action>
  <verify>
    <automated>test -f css/vendor/quill.bubble.css && test -f css/vendor/baguetteBox.min.css && test -f css/vendor/gutenberg.min.css && test -f css/print.css && test -f css/spacing.css && test ! -f public/quill.bubble.css && test ! -f public/flex.css && test ! -f dist/flex.css && test ! -f dist/reset.min.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All CSS files are in their correct source locations. public/ has no CSS files. Unreferenced flex.css and reset.min.css are deleted from both public/ and dist/.</done>
</task>

<task type="auto">
  <name>Task 2: Update template references and Vite config</name>
  <files>templates/header.html, templates/print.html, vite.config.js</files>
  <action>
1. Update templates/header.html:
   - Change `/dist/quill.bubble.css` to `/css/vendor/quill.bubble.css`
   - Change `/dist/baguetteBox.min.css` to `/css/vendor/baguetteBox.min.css`
   (Use {{ @BASE }} prefix as currently done in the template)

2. Update templates/print.html:
   - Change `/dist/gutenberg.min.css` to `/css/vendor/gutenberg.min.css`
   (Use {{ @BASE }} prefix as currently done in the template)
   - KEEP spacing.css and print.css references pointing at /dist/ -- Vite will output them there

3. Update vite.config.js -- add print.css and spacing.css as rollup inputs so Vite copies them to dist/:
   Add to build.rollupOptions.input:
     printStyles: 'css/print.css',
     spacingStyles: 'css/spacing.css',

   These will be processed by Vite and output to dist/ with the build.

4. Verify the build works by running: npm run dev
  </action>
  <verify>
    <automated>grep -q "css/vendor/quill.bubble.css" templates/header.html && grep -q "css/vendor/baguetteBox.min.css" templates/header.html && grep -q "css/vendor/gutenberg.min.css" templates/print.html && grep -q "printStyles" vite.config.js && npm run dev 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Template references point to css/vendor/ for vendor CSS. Vite config includes print.css and spacing.css as inputs. Build completes successfully with all CSS files in their expected output locations.</done>
</task>

</tasks>

<verification>
1. `npm run dev` completes without errors
2. All vendor CSS accessible: test -f css/vendor/quill.bubble.css && test -f css/vendor/baguetteBox.min.css && test -f css/vendor/gutenberg.min.css
3. Build outputs print and spacing CSS to dist/: ls dist/ | grep -E "print|spacing"
4. No CSS remains in public/: ls public/*.css 2>/dev/null | wc -l should be 0
5. No unreferenced CSS in dist/: test ! -f dist/flex.css && test ! -f dist/reset.min.css
6. Template references correct: grep "css/vendor" templates/header.html templates/print.html
</verification>

<success_criteria>
- Vendor CSS served from css/vendor/ (not dist/)
- print.css and spacing.css source lives in css/, output goes to dist/ via Vite
- No CSS files remain in public/
- flex.css and reset.min.css deleted everywhere
- Build completes successfully
- No broken stylesheet references in templates
</success_criteria>

<output>
After completion, create `.planning/quick/260321-pi3-move-css-files-out-of-dist-into-proper-s/260321-pi3-SUMMARY.md`
</output>
