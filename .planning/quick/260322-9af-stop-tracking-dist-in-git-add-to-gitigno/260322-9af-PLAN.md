---
phase: quick
plan: 260322-9af
type: execute
wave: 1
depends_on: []
files_modified: [.gitignore]
autonomous: true
must_haves:
  truths:
    - "dist/ directory is listed in .gitignore"
    - "No dist/ files appear in git status as tracked"
    - "Build output still works (dist/ exists on disk but is git-ignored)"
  artifacts:
    - path: ".gitignore"
      provides: "dist/ exclusion rule"
      contains: "dist"
  key_links: []
---

<objective>
Stop tracking the dist/ directory in Git and add it to .gitignore.

Purpose: dist/ contains build output (Vite manifest, compiled JS/CSS assets) that should not be version-controlled. These files are generated by `npm run dev` / `npm run prod` and change with every build, creating noisy diffs.
Output: Clean git index with no dist/ files tracked; .gitignore updated.
</objective>

<context>
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dist/ to .gitignore and remove from git index</name>
  <files>.gitignore</files>
  <action>
1. Edit .gitignore to add `dist` on a new line (place it near the other build output entries like `build` and `vendor`).
2. Remove dist/ from the git index without deleting the files on disk:
   `git rm -r --cached dist/`
3. Stage .gitignore:
   `git add .gitignore`
4. Commit with message: "chore: stop tracking dist/ in git, add to .gitignore"

Note: The `git rm --cached` flag removes files from tracking only — the actual dist/ directory remains on disk so the app continues to work.
  </action>
  <verify>
    <automated>git ls-files dist/ | wc -l | grep -q '^0$' && grep -q 'dist' .gitignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>No dist/ files in git index. .gitignore contains dist entry. dist/ directory still exists on disk.</done>
</task>

</tasks>

<verification>
- `git ls-files dist/` returns empty (no tracked files)
- `git status` shows no dist/ files as modified/untracked (they are ignored)
- `ls dist/` confirms files still exist on disk
- `grep dist .gitignore` confirms the rule exists
</verification>

<success_criteria>
dist/ is fully untracked by git and ignored via .gitignore. Build output files remain on disk.
</success_criteria>

<output>
After completion, update .planning/quick/260322-9af-stop-tracking-dist-in-git-add-to-gitigno/260322-9af-SUMMARY.md
</output>
