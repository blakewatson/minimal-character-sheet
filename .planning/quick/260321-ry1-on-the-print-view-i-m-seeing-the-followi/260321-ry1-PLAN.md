---
phase: quick
plan: 260321-ry1
type: execute
wave: 1
depends_on: []
files_modified:
  - js/languages/en.json
  - js/languages/de.json
  - js/components/Print.vue
autonomous: true
requirements: []
must_haves:
  truths:
    - "Print view shows 'Level 1 spells', 'Level 2 spells', etc. instead of 'Level {level} spells'"
  artifacts:
    - path: "js/languages/en.json"
      provides: "Explicit Level N spells translation entries for levels 1-9"
    - path: "js/languages/de.json"
      provides: "Explicit German Level N spells translation entries for levels 1-9"
    - path: "js/components/Print.vue"
      provides: "Updated template using explicit translation keys"
  key_links:
    - from: "js/components/Print.vue"
      to: "js/languages/en.json"
      via: "$t() lookup"
      pattern: "\\$t\\('Level \\d+ spells'\\)"
---

<objective>
Fix print view displaying literal "Level {level} spells" instead of interpolated spell level headings.

Purpose: The i18n `$t()` function does not support interpolation parameters. Print.vue passes `{ level: idx + 1 }` as a second argument which is silently ignored, resulting in the raw key "Level {level} spells" being displayed.

Output: Print view correctly shows "Level 1 spells" through "Level 9 spells" using explicit translation keys.
</objective>

<context>
@js/components/Print.vue (line 334 — the broken heading)
@js/languages/en.json (needs 9 new entries)
@js/languages/de.json (needs 9 new entries)
@js/i18n.js ($t only takes a key, no interpolation support)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit spell level entries to translation files and fix Print.vue</name>
  <files>js/languages/en.json, js/languages/de.json, js/components/Print.vue</files>
  <action>
1. In `js/languages/en.json`, add 9 entries after the existing "Level {level} spells" entry (keep the old entry for SpellGroup.vue which uses `.replace()`):
   - "Level 1 spells": "Level 1 spells"
   - "Level 2 spells": "Level 2 spells"
   - ... through "Level 9 spells": "Level 9 spells"

2. In `js/languages/de.json`, add 9 entries after the existing "Level {level} spells" entry:
   - "Level 1 spells": "Zauber der Stufe 1"
   - "Level 2 spells": "Zauber der Stufe 2"
   - ... through "Level 9 spells": "Zauber der Stufe 9"
   (Follow the existing German pattern from "Zauber der Stufe {level}")

3. In `js/components/Print.vue` line 334, change:
   ```
   {{ $t('Level {level} spells', { level: idx + 1 }) }}
   ```
   to:
   ```
   {{ $t('Level ' + (idx + 1) + ' spells') }}
   ```
   This constructs the key dynamically (e.g., "Level 1 spells") so $t() can look it up directly.
  </action>
  <verify>
    <automated>cd /workspace && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Print view heading renders "Level 1 spells" through "Level 9 spells" (or German equivalents) instead of "Level {level} spells". Build succeeds without errors.</done>
</task>

</tasks>

<verification>
- `npx vite build` completes without errors
- In en.json: keys "Level 1 spells" through "Level 9 spells" exist
- In de.json: keys "Level 1 spells" through "Level 9 spells" exist with German translations
- Print.vue no longer passes an interpolation object to $t()
</verification>

<success_criteria>
Print view spell headings display correctly with the spell level number instead of the literal placeholder "{level}".
</success_criteria>

<output>
After completion, update `.planning/quick/260321-ry1-on-the-print-view-i-m-seeing-the-followi/260321-ry1-SUMMARY.md`
</output>
