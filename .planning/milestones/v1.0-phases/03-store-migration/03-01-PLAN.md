---
phase: 03-store-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/store.js
autonomous: true
requirements:
  - STORE-01
  - STORE-07
  - STORE-08

must_haves:
  truths:
    - "store.js exports a reactive state object with all ~50 properties"
    - "store.js exports computed refs modifiers and proficiencyBonus"
    - "store.js exports all mutation functions as named exports"
    - "store.js exports getJSON as synchronous function returning JSON string"
    - "store.js exports initializeState and updateState as plain functions using Object.assign"
    - "initializeState assigns stable IDs to all spells across all 9 levels and cantripsList"
    - "initializeState normalizes collapsed:false on all 9 spell levels, not just lvl1"
  artifacts:
    - path: "js/store.js"
      provides: "Reactive composable store replacing Vuex"
      exports: ["state", "modifiers", "proficiencyBonus", "updateAbilityScore", "updateLevel", "updateBio", "updateVitals", "updateDeathSaves", "updateInitiative", "updateInspiration", "updateShortRests", "updateProficiencyOverride", "updateSkillProficiency", "updateSkillModifierOverride", "updatePassivePerceptionOverride", "updateSavingThrow", "updateAttacks", "addAttack", "deleteAttack", "sortAttacks", "updateTrackableField", "addTrackableField", "deleteTrackableField", "sortTrackableField", "updateCoins", "updateEquipment", "updateTextField", "addToListField", "updateListField", "deleteFromListField", "sortListField", "updateSpellInfo", "addSpell", "updateSpellName", "updateSpellPrepared", "updateSpellCollapsed", "deleteSpell", "updateSpellSlots", "updateExpendedSlots", "sortSpells", "getJSON", "initializeState", "updateState"]
  key_links:
    - from: "js/store.js"
      to: "js/level-data"
      via: "import levelData"
      pattern: "import levelData from './level-data'"
    - from: "js/store.js"
      to: "vue"
      via: "reactive, computed imports"
      pattern: "import.*reactive.*computed.*from 'vue'"
---

<objective>
Rewrite the Vuex store (js/store.js) as a Vue 3 reactive() composable with flat named exports.

Purpose: This is the foundation for the entire phase. All 18 components depend on the store's exported API. The store must expose the exact same state shape, computed values, and mutation functions -- just without the Vuex wrapper.

Output: A rewritten js/store.js that exports state (reactive object), modifiers/proficiencyBonus (computed refs), all mutation functions, getJSON (sync), initializeState, and updateState. Also fixes the SpellList ID bug (STORE-07) and confirms listMixin compatibility (STORE-08).
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-store-migration/03-CONTEXT.md
@.planning/phases/03-store-migration/03-RESEARCH.md

<interfaces>
<!-- Current store.js is a Vuex createStore({}) default export. After this plan,
     it becomes named exports from a reactive() module. The new API shape per D-01: -->

New export signature (what components will import):
```javascript
// State
export const state = reactive({ /* all ~50 properties */ });

// Computed refs (replace Vuex getters) -- access with .value in Options API
export const modifiers = computed(() => /* ... */);
export const proficiencyBonus = computed(() => /* ... */);

// Mutation functions (replace Vuex mutations) -- called directly
export function updateAbilityScore(payload) { /* ... */ }
export function updateBio(payload) { /* ... */ }
// ... all 25+ mutation functions

// Action functions (replace Vuex actions) -- called directly
export function getJSON() { return JSON.stringify(state); }  // sync, per D-02
export function initializeState(payload) { /* ... */ }
export function updateState(payload) { /* ... */ }
```

From js/level-data.js (imported by store):
```javascript
export default [ /* array of { lvl, xp, proficiency } objects */ ];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite store.js from Vuex to reactive() composable</name>
  <files>js/store.js</files>
  <read_first>
    - js/store.js (current Vuex store -- copy ALL state properties, ALL mutations, ALL getters, ALL actions exactly)
    - js/level-data.js (imported dependency)
    - .planning/phases/03-store-migration/03-RESEARCH.md (architecture patterns section has the skeleton)
    - .planning/phases/03-store-migration/03-CONTEXT.md (decisions D-01 through D-12)
  </read_first>
  <action>
Rewrite js/store.js from a Vuex `createStore({})` default export to a module with named exports using Vue 3 `reactive()` and `computed()`. The file must:

1. **Imports:** Replace `import { createStore } from 'vuex'` with `import { reactive, computed } from 'vue'`. Keep `import levelData from './level-data'`.

2. **State:** Create `export const state = reactive({ ... })` with ALL ~50 properties from the current `state:` object. Copy every property exactly as-is (id, slug, is_2024, readOnly, levelData, level, characterName, race, background, className, xp, alignment, hp, maxHp, tempHp, hitDie, totalHitDie, ac, speed, initiative, proficiencyOverride, inspiration, shortRests, deathSaves, conditions, concentration, abilities array with 6 objects, savingThrows array with 6 objects, skills array with 18 objects, attacks, trackableFields, coins array with 5 objects, equipmentText, proficienciesText, featuresText, personalityText, backstoryText, treasureText, organizationsText, notesText, passivePerceptionOverride, spClass, spAbility, spSave, spAttack, cantripsList, lvl1Spells through lvl9Spells).

3. **Computed refs (replace getters):** Per D-05:
   - `export const modifiers = computed(() => state.abilities.map(a => ({ ability: a.name, val: Math.floor(parseInt(a.score) / 2 - 5) })));`
   - `export const proficiencyBonus = computed(() => { if (state.proficiencyOverride !== null && state.proficiencyOverride !== undefined) return state.proficiencyOverride; var row = state.levelData.find(data => state.level === data.lvl); if (!row) return 2; return row.proficiency; });`

4. **Mutation functions:** Convert every mutation to an exported function. Remove the `state` parameter (use module-level `state` directly). Keep ALL validation logic (allowedFields checks, hasOwnProperty guards, etc.) EXACTLY as current. The functions are:
   - `updateAbilityScore(payload)` -- iterates state.abilities, sets score by name
   - `updateLevel(payload)` -- `state.level = payload.level`
   - `updateBio(payload)` -- validates against allowedFields ['characterName', 'race', 'background', 'className', 'xp', 'alignment'], sets `state[field] = payload.val`
   - `updateVitals(payload)` -- validates against allowedFields ['hp', 'maxHp', 'tempHp', 'hitDie', 'totalHitDie', 'ac', 'speed', 'conditions', 'concentration'], sets `state[field] = payload.val`
   - `updateDeathSaves(payload)` -- copies deathSaves, sets key/index/val
   - `updateInitiative(payload)` -- `state.initiative = payload`
   - `updateInspiration(payload)` -- `state.inspiration = payload`
   - `updateShortRests(payload)` -- `state.shortRests = payload`
   - `updateProficiencyOverride(payload)` -- `state.proficiencyOverride = payload`
   - `updateSkillProficiency(payload)` -- guard + set proficient/doubleProficient
   - `updateSkillModifierOverride(payload)` -- find skill, map to set modifierOverride (keep the console.log)
   - `updatePassivePerceptionOverride(payload)` -- `state.passivePerceptionOverride = payload`
   - `updateSavingThrow(payload)` -- findIndex by name, set proficient
   - `updateAttacks(payload)` -- find by id, map to set field
   - `addAttack(payload)` -- push new attack with id: Date.now()
   - `deleteAttack(payload)` -- filter by id
   - `sortAttacks(payload)` -- splice/reinsert by direction
   - `updateTrackableField(payload)` -- find by id, map to set field
   - `addTrackableField(payload)` -- push new field with id: Date.now()
   - `deleteTrackableField(payload)` -- filter by id
   - `sortTrackableField(payload)` -- splice/reinsert by direction
   - `updateCoins(payload)` -- guard i, set amount
   - `updateEquipment(payload)` -- `state.equipmentText = payload.val`
   - `updateTextField(payload)` -- guard hasOwnProperty, set `state[field] = payload.val`
   - `addToListField(payload)` -- guard hasOwnProperty, push `{ val: payload.val, id: Date.now().toString() }` (change from Math.random per Pitfall 7 in research)
   - `updateListField(payload)` -- guard + set val and collapsed
   - `deleteFromListField(payload)` -- guard + splice
   - `sortListField(payload)` -- findIndex by id, splice/reinsert
   - `updateSpellInfo(payload)` -- validate allowedFields, set `state[field] = payload.val`
   - `addSpell(payload)` -- guard + push item
   - `updateSpellName(payload)` -- guard + set name
   - `updateSpellPrepared(payload)` -- guard + set prepared
   - `updateSpellCollapsed(payload)` -- guard + set collapsed
   - `deleteSpell(payload)` -- guard + splice
   - `updateSpellSlots(payload)` -- guard + set slots
   - `updateExpendedSlots(payload)` -- guard + set expended
   - `sortSpells(payload)` -- findIndex by id, splice/reinsert
   - Do NOT export `replaceState` -- per D-04, this concept is dropped entirely.

5. **Action functions:**
   - `export function getJSON()` -- per D-02, synchronous: `return JSON.stringify(state);` (no Promise wrapper)
   - `export function initializeState(payload)` -- per D-03/D-04: same logic as current but instead of `commit('replaceState', { state })`, use `Object.assign(state, newState)` at the end. For the deep copy of defaults, define a `defaultState` const at module level (copy of the initial state object) and do `var newState = JSON.parse(JSON.stringify(defaultState))`. Keep ALL normalization logic (initiative default, weaponNotes, trackableFields properties, cantripsList collapsed). ADD per D-10: stable ID assignment for all 9 spell levels and cantripsList -- iterate `['lvl1Spells','lvl2Spells','lvl3Spells','lvl4Spells','lvl5Spells','lvl6Spells','lvl7Spells','lvl8Spells','lvl9Spells']`, for each level's spells array, assign `spell.id = (Date.now() + idx).toString()` if `!spell.id`. ADD per D-11: normalize `collapsed: false` for ALL 9 spell levels (current code only does lvl1). For cantripsList IDs use offset `(Date.now() + idx + 100).toString()`. Keep the id/slug/characterName/is_2024/readOnly assignments at the end.
   - `export function updateState(payload)` -- per D-04: same logic as current, end with `Object.assign(state, newState)` then `window.sheetEvent.emit('quill-refresh')`. For deep copy use `JSON.parse(JSON.stringify(defaultState))` as base. Keep weaponNotes and trackableFields normalization. Keep id/characterName/readOnly assignments.

6. **Keep** the `objectIsEmpty` helper function (not exported, used internally -- actually it's not used currently but keep it for safety).

7. **Remove:** The `createStore({})` wrapper, the `export default`, the `replaceState` mutation.

STORE-08 note: `js/mixins.js` (listMixin) uses `this.items[i] = value` and `this.items.push('')` -- direct array assignment, no Vue.set. It operates on component-local `data()`, not the store. No changes needed. STORE-08 is satisfied by current code.
  </action>
  <verify>
    <automated>cd /workspace && npx vite build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - js/store.js contains `import { reactive, computed } from 'vue'`
    - js/store.js contains `export const state = reactive({`
    - js/store.js contains `export const modifiers = computed(`
    - js/store.js contains `export const proficiencyBonus = computed(`
    - js/store.js contains `export function getJSON()` with body `return JSON.stringify(state)`
    - js/store.js contains `export function initializeState(payload)`
    - js/store.js contains `export function updateState(payload)`
    - js/store.js contains `Object.assign(state, newState)` (in both initializeState and updateState)
    - js/store.js does NOT contain `createStore` or `import { createStore` or `export default`
    - js/store.js does NOT contain `replaceState` as a function definition
    - js/store.js does NOT contain `commit(` (no Vuex commit calls)
    - js/store.js contains `Date.now() + idx` in initializeState (spell ID fix, STORE-07/D-10)
    - js/store.js contains iteration over all 9 spell levels for collapsed normalization (D-11): pattern `lvl2Spells.*lvl9Spells` or forEach over spellLevels array
    - js/store.js contains `export function updateBio(`
    - js/store.js contains `export function addAttack(`
    - js/store.js contains `export function sortSpells(`
    - js/store.js contains `export function addToListField(` with `Date.now().toString()` (not Math.random)
    - Build command `npx vite build` completes without errors (store is valid ES module even though components still use old Vuex patterns -- Vite tree-shakes unused exports)
  </acceptance_criteria>
  <done>store.js is a reactive() composable with all named exports, no Vuex dependency, stable spell IDs in initializeState, and collapsed normalization for all 9 spell levels. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx vite build` succeeds (store is valid ES module)
- `grep -c 'export function' js/store.js` returns >= 30 (all mutations + actions exported)
- `grep 'createStore\|from.*vuex' js/store.js` returns no matches
- `grep 'Date.now() + idx' js/store.js` returns a match (spell ID fix)
</verification>

<success_criteria>
js/store.js is fully rewritten as a reactive() composable with the exact same state shape, computed values, and mutation logic as the Vuex version. All exports are named. getJSON is synchronous. initializeState assigns stable spell IDs and normalizes collapsed for all 9 levels. Build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/03-store-migration/03-01-SUMMARY.md`
</output>
