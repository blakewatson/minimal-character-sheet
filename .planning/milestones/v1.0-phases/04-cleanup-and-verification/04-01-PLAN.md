---
phase: 04-cleanup-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix-manifest.json
  - js/store.js
  - js/app.js
  - js/print.js
  - CLAUDE.md
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "No Laravel Mix artifacts remain in the project root"
    - "No migration-era comments referencing 'replace Vuex' or 'replaces Vue.filter' exist in JS source"
    - "CLAUDE.md accurately describes Vue 3, reactive() store, Vite build tool, and mitt event bus"
    - ".gitignore covers dist/ output directory"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Accurate project documentation reflecting post-migration state"
      contains: "Vue.js 3"
    - path: ".gitignore"
      provides: "Git ignore rules covering Vite output"
      contains: "dist"
  key_links:
    - from: "CLAUDE.md"
      to: "vite.config.js"
      via: "Documentation references Vite as build tool"
      pattern: "Vite"
    - from: "CLAUDE.md"
      to: "js/store.js"
      via: "Documentation references reactive() composable store"
      pattern: "reactive"
---

<objective>
Remove dead migration artifacts and update project documentation to reflect the completed Vue 3 migration.

Purpose: The codebase should have no remnants of Laravel Mix, Vuex, or Vue 2 in documentation or comments. A new developer reading CLAUDE.md should understand the current stack without confusion.
Output: Clean codebase with accurate documentation.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead artifacts and clean migration comments</name>
  <files>mix-manifest.json, js/store.js, js/app.js, js/print.js</files>
  <read_first>mix-manifest.json, js/store.js, js/app.js, js/print.js, .gitignore</read_first>
  <action>
1. Delete `mix-manifest.json` from the project root. This is the last remaining Laravel Mix artifact. (CLEAN-01)

2. In `js/store.js`, update the three section-header comments that reference Vuex:
   - Line ~214: Change `// Computed refs (replace Vuex getters)` to `// Computed refs`
   - Line ~237: Change `// Mutation functions (replace Vuex mutations)` to `// Mutation functions`
   - Line ~574: Change `// Action functions (replace Vuex actions)` to `// Action functions`

3. In `js/app.js`, line ~19: Change `// Register signedNumString as a global property (replaces Vue.filter)` to `// Register signedNumString as a global property`

4. In `js/print.js`, line ~9: Change `// Register signedNumString as a global property (replaces Vue.filter)` to `// Register signedNumString as a global property`

5. Verify `.gitignore` contains `dist` (it already does -- CLEAN-03 is satisfied, just confirm). No changes needed.

6. Run `npx prettier --write js/store.js js/app.js js/print.js` to format modified files.
  </action>
  <verify>
    <automated>test ! -f mix-manifest.json && ! grep -q "replace Vuex\|replaces Vue.filter" js/store.js js/app.js js/print.js && grep -q "^dist" .gitignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - mix-manifest.json does not exist in the project root
    - js/store.js does not contain the string "replace Vuex"
    - js/app.js does not contain the string "replaces Vue.filter"
    - js/print.js does not contain the string "replaces Vue.filter"
    - js/store.js contains the comment "// Computed refs" (without Vuex reference)
    - js/store.js contains the comment "// Mutation functions" (without Vuex reference)
    - js/store.js contains the comment "// Action functions" (without Vuex reference)
    - .gitignore contains "dist"
  </acceptance_criteria>
  <done>All Laravel Mix artifacts deleted, all migration-era comments cleaned, .gitignore confirmed correct</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md to reflect post-migration stack</name>
  <files>CLAUDE.md</files>
  <read_first>CLAUDE.md, package.json, vite.config.js, js/store.js, js/app.js, index.php</read_first>
  <action>
Update CLAUDE.md to accurately reflect the completed migration. The file has GSD-managed sections (between GSD comment markers) and a top section that is manually maintained. Update ALL sections -- both GSD-managed and manual.

**Section: Architecture Overview (line 15)**
Change "D&D 5e character sheet web app with a PHP backend and Vue.js 2 frontend." to:
"D&D 5e character sheet web app with a PHP backend and Vue.js 3 frontend."

**Section: Frontend heading (line 27)**
Change `### Frontend (Vue.js 2)` to `### Frontend (Vue.js 3)`

**Section: Build line (line 29)**
Change `- **Build**: Laravel Mix (webpack) -- config in \`webpack.mix.js\`` to:
`- **Build**: Vite -- config in \`vite.config.js\``

**Section: CSS line (line 30)**
Change `Tailwind CSS v4 via \`@tailwindcss/postcss\`` to:
`Tailwind CSS v4 via \`@tailwindcss/vite\``

**Section: State line (line 35)**
Change `- **State**: Vuex store in \`js/store.js\` -- holds all character sheet data` to:
`- **State**: reactive() composable store in \`js/store.js\` -- holds all character sheet data`

**Section: Key Concepts, asset versioning line (line 55)**
Change `- Asset versioning via Mix manifest for cache busting (\`mix()\` helper in \`index.php\`)` to:
`- Asset versioning via Vite manifest for cache busting (\`vite()\` helper in \`index.php\`)`

**Section: GSD:project-start block (line 69)**
Change "The app is fully functional on Vue 2 + Vuex + Laravel Mix. This milestone migrates the frontend to Vue 3 + a \`reactive()\` composable store + Vite, with zero feature changes." to:
"The app runs on Vue 3 + a \`reactive()\` composable store + Vite."

**Section: GSD:stack-start -- Frameworks (lines 96-101)**
Replace these lines:
```
- Vue.js 2.6.7 -- Frontend reactive UI framework; entry points in `js/app.js`, `js/dashboard.js`, `js/print.js`
- Vuex 3.1.0 -- State management for Vue; store defined in `js/store.js`
- Tailwind CSS v4 via `@tailwindcss/postcss` ^4.1.18 -- No separate config file; all configuration done in `css/app.css` using `@theme` and `@custom-variant` directives
- Laravel Mix 6 -- Webpack wrapper; config in `webpack.mix.js`
- Sass 1.x / sass-loader 12.x -- SCSS preprocessing (available but primary styles use PostCSS)
- vue-loader 15.x / vue-template-compiler 2.7.14 -- Vue SFC compilation
```
With:
```
- Vue.js 3.5 -- Frontend reactive UI framework; entry points in `js/app.js`, `js/dashboard.js`, `js/print.js`
- reactive() composable -- State management in `js/store.js` (plain Vue 3 reactivity, no external state library)
- Tailwind CSS v4 via `@tailwindcss/vite` ^4.2.2 -- No separate config file; all configuration done in `css/app.css` using `@theme` and `@custom-variant` directives
- Vite 8 -- Build tool; config in `vite.config.js`
- @vitejs/plugin-vue 6 -- Vue 3 SFC compilation
```

**Section: GSD:stack-start -- Key Dependencies (lines 105-106)**
Remove the `vuex` line:
```
- `vuex` 3.1.0 -- All frontend state
```
Change:
```
- `vue` 2.6.7 -- All frontend UI
```
To:
```
- `vue` 3.5 -- All frontend UI
```

**Section: GSD:stack-start -- Configuration (lines 120-122)**
Replace:
```
- `webpack.mix.js` -- Defines all JS/CSS compilation steps and versioning
- Output directory: `dist/`
- Asset versioning manifest: `dist/mix-manifest.json` (consumed by `mix()` helper in `index.php`)
```
With:
```
- `vite.config.js` -- Defines all JS/CSS compilation steps and versioning
- Output directory: `dist/`
- Asset versioning manifest: `dist/.vite/manifest.json` (consumed by `vite()` helper in `index.php`)
```

**Section: GSD:conventions-start -- Import Organization (line 170-171)**
Replace:
```
- Plugins registered at app entry: `Vue.use(i18nPlugin)` in `js/app.js`
- Filters registered globally: `Vue.filter('signedNumString', signedNumString)`
```
With:
```
- Plugins registered at app entry: `app.use(i18nPlugin)` in `js/app.js`
- Global properties: `app.config.globalProperties.$signedNumString = signedNumString`
```

**Section: GSD:conventions-start -- Function Design (lines 198-203)**
Remove or replace Vuex-specific lines:
```
- No JSDoc on Vue component methods or Vuex mutations
```
becomes:
```
- No JSDoc on Vue component methods or store functions
```

Replace:
```
- Vuex mutations and actions receive a single `payload` object: `{ field, val, id }`
```
With:
```
- Store mutation functions receive individual parameters: `updateBio(field, val)`, `addAttack()`
```

Remove:
```
- Vuex mutations return nothing (side-effect only)
```

**Section: GSD:conventions-start -- Module Design (line 207)**
Replace:
```
- Default export for Vuex store: `export default new Vuex.Store({...})`
```
With:
```
- Named exports for store: `export { state, modifiers, proficiencyBonus, updateBio, ... }`
```

**Section: GSD:architecture-start -- Pattern Overview (lines 218, 244, 252, 259)**
All references to "Vue.js 2 + Vuex" become "Vue.js 3 + reactive() store":
- Line 218: "Vue.js 2 + Vuex SPA" -> "Vue.js 3 SPA"
- Line 244: "Vue.js 2 SPA mounted at `#sheet`, Vuex store (`js/store.js`)" -> "Vue.js 3 SPA mounted at `#sheet`, reactive() store (`js/store.js`)"
- Line 252: "All character data lives in Vuex store" -> "All character data lives in reactive() store"
- Line 253-255: Replace Vuex-specific data flow lines:
  ```
  - Mutations are explicit and named per field type (e.g., `updateBio`, `updateVitals`, `addAttack`)
  - `replaceState` mutation is used for bulk initialization and polling updates
  ```
  With:
  ```
  - Mutation functions are named per field type (e.g., `updateBio`, `updateVitals`, `addAttack`)
  - `initializeState` uses Object.assign for bulk state hydration and polling updates
  ```
- Line 259: "watches Vuex mutations for autosave" -> "uses watch(state, { deep: true }) for autosave"
- Line 261: "Flat state object, named mutations per field group, `initializeState` / `updateState` actions for hydration" -> "Flat reactive() state object, named mutation functions, `initializeState` / `updateState` for hydration"

After all edits, run: `npx prettier --write CLAUDE.md`
  </action>
  <verify>
    <automated>! grep -q "Vue.js 2\|Vue 2\|Vuex\|Laravel Mix\|webpack\.mix\|mix-manifest\|Vue\.use\|Vue\.filter\|Vue\.set\|vue-loader\|vue-template-compiler\|sass-loader\|@tailwindcss/postcss" CLAUDE.md && grep -q "Vue.js 3" CLAUDE.md && grep -q "reactive()" CLAUDE.md && grep -q "Vite" CLAUDE.md && grep -q "vite.config.js" CLAUDE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md does not contain the string "Vue.js 2"
    - CLAUDE.md does not contain the string "Vuex" (case-sensitive)
    - CLAUDE.md does not contain the string "Laravel Mix"
    - CLAUDE.md does not contain the string "webpack.mix"
    - CLAUDE.md does not contain the string "mix-manifest"
    - CLAUDE.md does not contain the string "Vue.use"
    - CLAUDE.md does not contain the string "Vue.filter"
    - CLAUDE.md does not contain the string "vue-loader"
    - CLAUDE.md does not contain the string "vue-template-compiler"
    - CLAUDE.md does not contain the string "sass-loader"
    - CLAUDE.md does not contain the string "@tailwindcss/postcss"
    - CLAUDE.md contains "Vue.js 3" in the Architecture Overview heading area
    - CLAUDE.md contains "reactive()" describing the store
    - CLAUDE.md contains "Vite" as build tool
    - CLAUDE.md contains "vite.config.js" as config file
    - CLAUDE.md contains "vite()" as the PHP helper
    - CLAUDE.md contains "mitt" or "event bus" for cross-component communication
    - npm run prod exits 0 (build still works after changes)
  </acceptance_criteria>
  <done>CLAUDE.md accurately describes Vue 3, reactive() composable store, Vite build tool, mitt event bus, and all current dependencies with no references to Vue 2, Vuex, or Laravel Mix</done>
</task>

</tasks>

<verification>
1. `test ! -f mix-manifest.json` -- no dead Mix artifacts
2. `grep -c "Vuex\|Laravel Mix\|Vue.js 2\|webpack.mix\|vue-template-compiler" CLAUDE.md` returns 0
3. `grep -c "replace Vuex\|replaces Vue.filter" js/store.js js/app.js js/print.js` returns 0
4. `npm run prod` exits 0 -- build is not broken
5. `grep -q "^dist" .gitignore` -- dist is ignored
</verification>

<success_criteria>
- Zero references to Vue 2, Vuex, Laravel Mix, webpack.mix.js, or mix-manifest.json in CLAUDE.md
- Zero migration-era "replace Vuex" / "replaces Vue.filter" comments in JS source files
- mix-manifest.json deleted from project root
- Production build succeeds
- .gitignore covers dist/
</success_criteria>

<output>
After completion, create `.planning/phases/04-cleanup-and-verification/04-01-SUMMARY.md`
</output>
