---
phase: 04-cleanup-and-verification
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified: []
autonomous: false
requirements:
  - CLEAN-04
  - CLEAN-05

must_haves:
  truths:
    - "Character sheet editing works: user can edit all fields and changes persist"
    - "Print view renders correctly with all character data"
    - "Dashboard displays sheets, allows toggling public state, allows deleting sheets"
    - "Public read-only sheets load and display correctly"
    - "Autosave fires after edits without errors in browser console"
    - "Quill rich text editors accept input and save content"
    - "Skill proficiency toggles work correctly"
    - "Spell list add/remove operations work"
    - "Attack list add/remove operations work"
    - "Equipment list operations work"
  artifacts: []
  key_links: []
---

<objective>
Verify that every user-facing feature works identically to the pre-migration version.

Purpose: The entire migration is meaningless if features regressed. This plan confirms zero regressions across all views and interactive features.
Output: Human-verified confirmation that all features work correctly.
</objective>

<execution_context>
@/workspace/.claude/get-shit-done/workflows/execute-plan.md
@/workspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cleanup-and-verification/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build production assets and verify no build warnings</name>
  <files></files>
  <read_first>package.json, vite.config.js</read_first>
  <action>
1. Run `npm run prod` and capture output. Verify:
   - Exit code is 0
   - No warnings in output (ignore "computed gzip size" info lines)
   - All expected output files are present: dist/assets/app-*.js, dist/assets/print-*.js, dist/assets/dashboard-*.js, dist/assets/styles-*.css, dist/assets/i18n-*.js
   - dist/.vite/manifest.json exists and contains entries for js/app.js, js/print.js, js/dashboard.js, css/app.css

2. Run `npm run dev` and verify it also exits 0 with no errors.
  </action>
  <verify>
    <automated>npm run prod 2>&1 | tail -1 | grep -q "built in" && ls dist/.vite/manifest.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - npm run prod exits with code 0
    - dist/.vite/manifest.json exists
    - dist/assets/ contains at least 5 files (3 JS chunks + 1 CSS + i18n chunk)
    - npm run dev exits with code 0
  </acceptance_criteria>
  <done>Both dev and prod builds complete successfully with no errors or warnings</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all views and features work correctly</name>
  <what-built>The complete Vue 3 migration: build tool (Vite), framework (Vue 3), store (reactive() composable), and cleanup of all migration artifacts. The application should function identically to the pre-migration Vue 2 version.</what-built>
  <how-to-verify>
Build and deploy the app, then test each area:

**1. Character Sheet Editing (CLEAN-04, CLEAN-05)**
- Open a character sheet in the browser
- Edit the character name -- verify it saves (check for no console errors)
- Edit ability scores -- verify modifiers update with signed numbers (+2, -1, etc.)
- Toggle skill proficiencies -- verify checkmarks appear/disappear
- Switch between tabs (Skills, Spells, Equipment, Bio, etc.)
- Verify autosave: make an edit, wait 2-3 seconds, refresh the page -- the edit should persist

**2. Quill Rich Text Editors (CLEAN-05)**
- Open the Bio tab or Equipment section
- Type in a Quill editor (backstory, equipment notes, features)
- Apply formatting (bold, italic)
- Refresh the page -- formatted text should persist

**3. List Operations (CLEAN-05)**
- Add a spell to the spell list -- verify it appears
- Remove a spell -- verify it disappears
- Add an attack -- verify it appears
- Remove an attack -- verify it disappears
- Add equipment items
- Verify the "auto-append empty row" behavior works (typing in the last empty row adds a new one)

**4. Print View (CLEAN-04)**
- Navigate to the print view (/print/SLUG)
- Verify all character data renders
- Verify spell levels display correctly (not showing interpolation like {{ }})
- Verify skill checkmarks render

**5. Dashboard (CLEAN-04)**
- Visit /dashboard
- Verify sheets are listed
- Toggle a sheet's public/private state
- Verify the toggle persists on refresh

**6. Public Read-Only Sheet (CLEAN-04)**
- If you have a public sheet, open its public URL in an incognito window
- Verify it loads in read-only mode
- Verify no edit controls are visible

**7. Browser Console**
- Throughout all testing, keep the browser console open
- There should be zero Vue warnings and zero JavaScript errors
  </how-to-verify>
  <resume-signal>Type "approved" if all features work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Production build succeeds with no errors
2. Human confirms all views render correctly
3. Human confirms all interactive features work
4. Human confirms no browser console errors
</verification>

<success_criteria>
- Production and development builds both succeed
- Human verifies: sheet editing, print view, dashboard, public sheets all work
- Human verifies: autosave, Quill editors, skill/spell/attack/equipment list operations all work
- No JavaScript errors or Vue warnings in browser console
</success_criteria>

<output>
After completion, create `.planning/phases/04-cleanup-and-verification/04-02-SUMMARY.md`
</output>
