export const global = /** @type {typeof window & ({
 *   is_2024: boolean,
 *   characterName: string,
 *   markdownit: any,
 *   md: any,
 *   sheetEvent: import('mitt').Emitter<Record<import('mitt').EventType, unknown>>
 * })}
 */ (window);
